import { CalculoDirective } from './calculo.directive';

describe('CalculoDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculoDirective();
    expect(directive).toBeTruthy();
  });
});
